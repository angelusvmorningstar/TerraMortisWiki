# Deferred Work

Real-but-not-now findings from code review, kept here so they aren't lost or silently re-litigated in a later story.

## From Story 1-1 (repo-scaffold-and-css-tokens) review

1. **`--surf1` has no `[data-theme="dark"]` override** in `public/css/theme.css`. Inherited faithfully from `../TM Suite/public/css/theme.css`, which has the same gap upstream — this is not a porting error. Currently inert (`--surf1` is unused in `base.css`). Fix here (and ideally upstream in TM Suite too) the first time a story actually consumes `var(--surf1)` in a dark-mode-visible element.
2. **`express.static` is registered before the `/` route**, so a future `public/index.html` would silently shadow the `homePage()` handler with no error. Latent only — no such file exists yet. Worth a comment or an ordering fix the first time a story adds static HTML under `public/`.
3. **No 404 handler or centralised error-handling middleware.** Acceptable for a one-route skeleton; add when routes with real async I/O (auth, dossier views) land in stories 1-3/2-1, so an unhandled rejection doesn't fall through to Express's default error page.
4. **Google Fonts CDN has no local fallback, SRI, or CSP.** Low severity for a placeholder page; revisit as part of a production-hardening pass (also consider `helmet` for basic security headers) before this is player-facing for real.
5. **The `isMain` check (`process.argv[1] === fileURLToPath(import.meta.url)`) is fragile** across symlinked or extensionless invocations (the `es-main` idiom exists precisely for this). Fine for the documented `npm start` / `node server/index.js` invocation; revisit if the deploy target ever launches the process differently.
6. **`PORT=0` produces a misleading startup log** (`http://localhost:0`) since Node binds a random ephemeral port in that case but the log doesn't reflect it. Cosmetic; not worth a fix unless something starts scripting against the logged URL.

## From Story 1-2 (mongo-snapshot-script) review

1. **BLOCKING FOR STORY 2-1 — `data/snapshot.json` carries every character's full document and every `character_dossier` fact (including `st_hidden: true` secrets) in the clear.** This is correct and intentional for this story — architecture.md places redaction at the Express serving layer, not the snapshot — but it means the committed file has no protection of its own. Story 2-1's implementation MUST: (a) implement the owner-full vs whitelist-summary projection server-side, never trusting the client; (b) never `express.static()` (or otherwise directly serve) the `data/` directory or `snapshot.json` itself; (c) be reviewed with this specifically in mind — a single misconfigured static route would leak every character's secrets and every player's `st_hidden` facts at once. Do not let story 2-1's code review treat this as optional.
2. **`toPlain()` only special-cases `ObjectId` and `Date`.** Other BSON types (`Decimal128`, `Long`, `Binary`, `Timestamp`) would serialize as their internal object structure rather than a sensible plain value — no crash, silent data corruption. None of the four collections currently store these types (verified against live data), so this is dormant risk, not an active bug. Revisit if a future collection or field introduces a non-string numeric/binary BSON type.
3. **The AC #3 "no writes" test is a lexical source-scan, not a runtime guarantee.** It was hardened this story to also catch `$out`/`$merge`/`db.command`/`runCommand`/bulk-op-initializers, but a lexical scan can still be evaded by sufficiently indirect code (e.g. a dynamically-constructed method name). The only true enforcement is the Atlas database user's read-only IAM role, which is a manual Atlas-console step Angelus performs outside this codebase (see README). Treat the test as a tripwire against accidental writes, not as the actual security boundary.

## From Story 1-2 (mongo-read-client, live version) review

(Distinct from the retired `## From Story 1-2 (mongo-snapshot-script) review` section above — that one is the superseded snapshot story. This section is the LIVE `server/db.js` + `server/mongo-store.js` read client.)

1. **`connectDb` has no in-flight/connecting guard.** Two concurrent callers that both arrive before the first `await client.connect()` resolves each see `db` still `null`, each construct a `new MongoClient`, and the second assignment to the module-level `client` orphans the first connection (it is never `close()`d). Not a live bug: `connectDb` is called exactly once, serially, at boot (`server/index.js` `start()`), and `setTestDb` short-circuits it in tests. Revisit only if a lazy or per-request connect path is ever introduced, in which case wrap the connect in a shared in-flight promise (`if (connecting) return connecting`).
2. **Lexical no-writes guard is a tripwire, not the boundary.** `mongo-store.test.js`'s AC #4 scan covers only `db.js`/`mongo-store.js` source and can be evaded by sufficiently indirect (dynamically-named) code; a caller elsewhere that holds a `getCollection()` handle is entirely outside its reach. The real read-only enforcement is the Atlas database user's read-only IAM role (a manual Atlas-console step, see README). This mirrors deferred-work item 3 under the retired snapshot-story heading and is restated here so the live module carries the same caveat forward — treat the test as protection against accidental writes in these two files, not as the security boundary.

## From Story 1-3 (discord-oauth-reuse) review

1. **In-memory token cache (`server/middleware/auth.js`) grows unbounded** — expired entries are never swept, only overwritten or deleted on an explicit 401. Not a confidentiality issue (cached identity is only ever the identity Discord itself returned for that exact token), just unbounded memory growth over the process lifetime. Negligible at this app's real scale (dozens of players); revisit only if that assumption ever changes.
2. **`snapshot-store.js` accessors (`getPlayers`, `getCharacters`, etc.) return live references into the in-memory snapshot, not copies.** `buildUserFromPlayer` already defends itself by copying `character_ids`, so there is no present bug. But any future route that mutates a returned character/player object in place would silently corrupt the shared boot snapshot for every subsequent request. Worth a shallow-copy or `Object.freeze` pass on the accessors before story 2-1/2-2 start reading `getCharacters()`/`getDossiers()` directly, so this can't become a footgun by omission.
3. **Callback route network-failure handling was ported without the middleware's try/catch, then patched here** — now consistent (both wrap their Discord `fetch` calls). Noting only so a future refactor that touches one doesn't quietly drop the other out of parity again.

## From Story 1-4 (netlify-render-split) review

1. ~~**Local-dev cross-origin OAuth cannot complete...**~~ **RESOLVED** (2026-07-17): a real local end-to-end OAuth path was wanted after all. Fixed both gaps this note called out: `credentials: 'include'` added to the callback fetch in `public/login.html`, and the login button's `href` is now built from `API_BASE` instead of a bare relative `/auth/discord` (was 404ing against the static server on `:8080`). Found live while testing local OAuth for the first time.
2. **`getUser` in `public/js/auth/login-core.js` does an unguarded `JSON.parse`** on `tm_auth_user`; a corrupted/tampered storage value throws out of the page's `init()` and blanks the page rather than falling back to the logged-out state. Faithfully mirrors `../TM Suite/public/js/auth/discord.js` (same unguarded parse upstream), so it is not a porting defect and is inert unless storage is externally corrupted. Wrap the parse in a try/catch returning `null` (and ideally `clearAuth`) the first time a story depends on graceful recovery from a bad stored session; consider fixing upstream in TM Suite too.

## From Story 2-1 (character-dossier-views) review

1. **Summary-tier facts are passed through whole, not allowlist-projected (F2).** `filterFactsForViewer` gates WHICH facts a viewer sees correctly (the `st_hidden`/`revealed_to` decision is airtight and independently re-verified), but a visible fact is returned as its ENTIRE object — including the schema's ST-facing metadata sub-fields `note` ("ST-facing note", e.g. "sheet has fewer touchstones", per `../TM Suite/server/schemas/character_dossier.schema.js`), `clash`, `sheet_value`, `sheet_field`, `source`, and (on a shown fact) `revealed_to` itself. This is a THIRD, undefined channel: neither an owner-only character field nor an `st_hidden` fact. It violates no AC in Story 2-1 (AC #5 scopes fact visibility solely to `st_hidden`), the frontend renders only `tag`+`value`, and no sensitive data rides these sub-fields on any non-hidden fact in the live data today — so there is no present exposure. BUT it is the exact allowlist-vs-passthrough principle this story is built on, applied to character fields and NOT to fact objects, and AC #6's rule is "the server must not send what the viewer shouldn't see", not "rely on the client to render a safe subset". **Fix in Story 2-2** (which renders facts more richly and will read the same fact channel): project each visible fact field-by-field to a public allowlist (`tag`, `value`, plus any deliberately-public fields), mirroring `summariseCharacter`. Until then, do NOT author sensitive content into the `note`/`sheet_value` fields of a fact whose `st_hidden` is not `true`.
2. ~~**`public/login.html` `<title>` still carries an em-dash**~~ **RESOLVED**: now `Terra Mortis Wiki: Log in` (colon).

## From Story 3-1 (world-status-layout) review

1. **A covenant with whitespace or case variance falls into the "No covenant recorded" catch-all rather than its covenant group (F2).** In `public/js/world/court-view.js`, both `covenantSlug` and `buildWhosWho`'s grouping key on an EXACT `COVENANT_ICON_SLUG` lookup with no trim or case-fold, so a value like `"Invictus "` (trailing space) or `"invictus"` (lowercase) is not recognised and the character is grouped under "No covenant recorded" instead of its real covenant. No crash and no broken icon (the catch-all renders no crest), so it is cosmetic data-hygiene only. It mirrors the codebase-wide non-trimming, exact-match convention (`public/js/characters/list.js` does the identical `COVENANT_ICON_SLUG[c.covenant]` lookup) and is the covenant sibling of Story 2-2's whitespace-honorific finding. Whether any such value exists in live data is unknown (the live-data covenant enumeration is Angelus/Peter's read-only Mongo step). Not patched in review: trimming or case-folding here would diverge from the exact-match convention shared with `list.js`, and there is no evidence such a value exists. **Fold into the same pass that runs the deferred live-covenant/honorific enumeration**: if any whitespace or mis-cased covenant surfaces, either clean the data upstream or normalise the lookup in ONE shared place (`covenant-icons.js`) so `list.js` and the Court page stay consistent.

2. **All three Court-page sections default open; Who's Who at live scale dumps the full roster on load (F3).** `public/js/world/court.js` renders every section as `<details open>` per AC #2 (mockup-locked). At the live counts (Court 19, Regencies 5, Who's Who 37) that is roughly 61 rows visible on load. The mockup looked compact only because its fixture Who's Who was 10 rows, so the apparent reasonableness was a fixture-size artefact, not a validated decision at scale. NOT a code defect: the code faithfully implements the AC-mandated default-open state, the sections are native `<details>` (a viewer can collapse any of them with keyboard or mouse for free), and an all-visible "Who's Who" reference is arguably a feature (Ctrl-F over the whole roster). Not patched: defaulting Who's Who to collapsed would VIOLATE AC #2. A UX watch-item for a design/ST call if the default-open roster proves unwieldy in play (e.g. default Who's Who closed while keeping Court and Regencies open, which would need the AC re-opened first).

## From Story 2-2 (world-tab) review

1. **A whitespace-only `honorific` forms a phantom title group (F1).** In `server/routes/world.js`, `buildWorldView`'s honorific skip guard is `honorific === null || honorific === undefined || honorific === ''` — it treats only the empty string as "no honorific". A value of pure whitespace (e.g. `"  "`) is truthy-distinct from `''`, so it becomes its own group `Map` key and `public/js/world/world.js` renders a title group with a blank-looking heading. AC #3's intent is that an "empty/absent honorific simply contributes to no title group (honest gap)". No leak and no crash: the holder is still allowlist-projected (`summariseHolder`), so this is cosmetic data-hygiene only, and it mirrors the codebase-wide non-trimming convention (`displayName` / `summariseCharacter` do not trim `honorific` either). Not patched in the review because trimming here would diverge from the untrimmed `displayName` convention, and whether any whitespace honorific exists in live data is unknown — the MANDATORY live-data honorific enumeration (Dev Notes verification step) was deliberately not run this cycle (Angelus/Peter's live-Mongo step). **Fold into the same pass that runs the deferred live-honorific enumeration**: if any whitespace/blank honorific surfaces, either clean the data upstream or tighten the guard to `String(honorific).trim() === ''` (and confirm the trim direction matches whatever `displayName` does at that time).

## From Story 3-2 (covenant-clan-status-ladders) review

1. **Covenant/clan name comparison is exact-match, so whitespace or case variance splits a faction (F1).** In `server/routes/status.js`, `buildStatusView` compares covenant and clan strings by exact value only: the covenant list uses `Object.entries(status.covenant)` keys and `c.covenant === cov`; the clan list uses `c.clan === clan`. There is no `trim()` or case-fold anywhere, mirroring the codebase-wide non-trimming convention (`public/js/world/court-view.js` and `public/js/characters/list.js` do the same exact-match `COVENANT_ICON_SLUG[c.covenant]` lookup, and TM Suite's `covenantListFor`/`clanRowsFor`, the ported source of truth, also do not case-fold). This is the covenant/clan sibling of Story 3-1's F2 and Story 2-2's F1 whitespace family. **Crucially, the direction here is fail-safe, not a leak.** A viewer's entitled ladders are derived only from the exact covenant/clan strings on the viewer's OWN character(s), so a whitespace/case-variant value can never mint a ladder for a faction the viewer does not belong to. It can only (a) under-populate a ladder (a rival whose primary is `"Invictus "` misses the `=== "Invictus"` membership test and is silently omitted, i.e. less exposure), or (b) split the viewer's own faction into two near-duplicate ladders if the viewer's own characters carry inconsistent spellings. No cross-faction leak, no crash. Not patched in review: trimming/case-folding here would diverge from the exact-match convention shared with `list.js`/`court-view.js` and from the TM Suite source it ports, and whether any such value exists in live data is unknown (the live covenant/clan enumeration is Angelus/Peter's read-only Mongo step). **Fold into the same pass that runs the deferred live-covenant/honorific enumeration** (see Story 3-1 F2 and Story 2-2 F1): if any whitespace or mis-cased covenant/clan surfaces, either clean the data upstream or normalise the comparison in ONE shared place so `list.js`, `court-view.js`, and `status.js` stay consistent.

2. **The Covenant/Clan section count badge sums rows across ladders, so a character in two of the viewer's owned faction ladders is counted twice (F2).** In `public/js/world/status.js`, `covenantSectionHtml`/`clanSectionHtml` compute the badge as `ladders.reduce((n, l) => n + l.rows.length, 0)`. For the common single-covenant/single-clan viewer this is exact. For a hypothetical multi-character owner whose union yields two covenant ladders, a character appearing in both (e.g. primary in one covenant and cross-covenant standing in the other) is counted once per ladder, so the badge over-counts distinct characters. AC #15 asks the badge to be "the number of characters in that section"; strictly this is the number of ROWS. No leak, no crash, and inert at the current scale (every player owns exactly one character today, so the union is always a single ladder per section, per the story's own Dev Notes). Not patched: the code faithfully renders row counts and the divergence only manifests under the multi-character union that does not exist in live data yet. Revisit if/when a genuine multi-character owner appears, deciding then whether the badge should count distinct `_id`s or remain a row tally.

3. **Two correct-but-unasserted ordering/dedup paths (F3, test-coverage).** `buildStatusView`'s row sort tie-breaks equal `value` by `sortName` (`b.value - a.value || sortNameKey(a).localeCompare(sortNameKey(b))`), and the covenant-list/clan-set dedup collapses two owned characters sharing a covenant/clan into a single ladder (`!covenantList.includes(cov)` / `!clanList.includes(clan)`). Both are implemented correctly, and the dedup is exercised indirectly by the multi-character union test (charB's Invictus standing de-duplicates against charA's primary Invictus for viewer 999). However, no test asserts the equal-value `sortName` tie-break directly (every fixture ladder has strictly distinct values), and no test asserts the exactly-two-owned-characters-in-the-same-covenant single-ladder case in isolation. Not a defect and not patched (the logic is correct and mirrors the vetted `world.js`/`characters.js` sort convention); logged so a future refactor of the sort or dedup path adds a directly discriminating assertion rather than relying on the incidental coverage.
